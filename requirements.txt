# Core
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
accelerate>=0.28.0
numpy>=1.24
scipy>=1.10
pillow>=10.0
pyyaml>=6.0
tqdm>=4.66
pandas>=2.0
scikit-learn>=1.3

# NLP utilities
nltk>=3.8
rouge-score>=0.1.2
sacrebleu>=2.4
bert-score>=0.3.13

# Clinical metrics (optional; users must install per-project weights separately)
# radgraph  - see https://pypi.org/project/radgraph/  (requires transformers + cached weights)
# rate-score / ratescore - see paper https://github.com/wjhou/RaTEScore

# Logging / experiment tracking (optional)
tensorboard>=2.15
wandb>=0.16

# Image utilities
opencv-python-headless>=4.8
